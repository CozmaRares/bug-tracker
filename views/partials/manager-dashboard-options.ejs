<% if(role.toUpperCase() === "ADMIN" || role.toUpperCase() === "MANAGER") { %>
<div>
  <h6>management</h6>
  <% if(role.toUpperCase() === "ADMIN") { %> <%-
  include("admin-dashboard-options") %> <% } %>
  <a href="/projects" data-projects>
    <li>
      <div>
        <i class="fa-solid fa-desktop"></i>
      </div>
      manage projects
    </li>
  </a>
</div>
<% } %>
